/** @type {import('next').NextConfig} */
const nextConfig = {
  // Здесь остаются ваши настройки, если они были
  // reactStrictMode: true,
  // images: { domains: ['example.com'] },
};

module.exports = nextConfig;